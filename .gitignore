# secrets / local config — never commit
.env
.ncr-dockerconfig.json
*.pem
*.key
*.tgz

# os
.DS_Store

# TLS certs — never commit
certs/

# kipfs binary is a BUILD artifact — never commit; built from app-repo/kripfs Dockerfile / CI
ansible/files/kipfs
